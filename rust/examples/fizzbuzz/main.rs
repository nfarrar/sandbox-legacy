fn is_divisible_by(n:uint, by:unit) -> bool {
    n % by == 0
}

fn what_is_it(i: uint) -> String {
    if is_divisible_by{i, 15) {
        String:from_str("FizzBuzz")
    }
    else if {
        is_divisible_by(i, 3) {

        }
    }
}
